import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const sql=readFileSync(resolve(process.cwd(),'supabase/migrations/202608010003_phase13_financial_sync.sql'),'utf8');
describe('Phase 13 financial SQL',()=>{it('supports only implemented financial modules',()=>{for(const name of['utang_entries','gcash_transactions','bills','employees','payroll_entries','vault_transactions'])expect(sql).toContain(name);expect(sql).not.toContain('supplier_payments');});it('uses operation receipts and immutable inserts',()=>{expect(sql).toContain('process_financial_operation');expect(sql).toContain('from public.sync_operations where operation_id=op_id');expect(sql).toContain("'duplicate',true");expect(sql).toContain('insert into public.utang_entries');expect(sql).toContain('insert into public.vault_transactions');expect(sql).toContain('Manager role is required for sensitive financial operations');});it('adds all supported ledgers to incremental pull',()=>{expect(sql).toContain("select 'payroll_entries'");expect(sql).toContain("select 'gcash_transactions'");});});