import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const sql=readFileSync(resolve(process.cwd(),'supabase/migrations/202608030002_phase19_security_hardening.sql'),'utf8');
describe('Phase 19 security hardening',()=>{it('forces RLS and removes anonymous table access',()=>{expect(sql).toContain('force row level security');expect(sql).toContain('revoke all on table public.%I from anon');});it('limits user text and audit payload sizes without rewriting existing rows',()=>{expect(sql).toContain('products_name_length');expect(sql).toContain('audit_details_size');expect(sql.match(/not valid/g)?.length).toBeGreaterThanOrEqual(8);});it('removes public and anonymous execution from RPCs that exist',()=>{expect(sql).toContain('pg_get_function_identity_arguments');expect(sql).toContain("'process_sync_operations'");expect(sql).toContain("'revoke_store_device'");expect(sql).toContain("'cleanup_sync_receipts'");expect(sql).toContain("from public,anon");});});