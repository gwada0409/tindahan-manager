import{readFileSync}from'node:fs';import{resolve}from'node:path';import{describe,expect,it}from'vitest';const sql=readFileSync(resolve(process.cwd(),'supabase/migrations/202608030001_phase18_retention_indexes.sql'),'utf8');
describe('Phase 18 retention and indexes',()=>{it('uses bounded authenticated receipt cleanup',()=>{expect(sql).toContain('private.is_active_store_member');expect(sql).toContain('p_retention_days not between 7 and 365');expect(sql).toContain('p_limit not between 1 and 5000');expect(sql).toContain('limit p_limit');expect(sql).toContain('revoke all on function public.cleanup_sync_receipts');});it('indexes retention, activity, and open reconciliation queries',()=>{expect(sql).toContain('sync_operations_retention_idx');expect(sql).toContain('devices_store_activity_idx');expect(sql).toContain('inventory_reconciliation_open_idx');expect(sql).toContain('resolved_at,created_at desc');expect(sql).not.toContain('detected_at');});});